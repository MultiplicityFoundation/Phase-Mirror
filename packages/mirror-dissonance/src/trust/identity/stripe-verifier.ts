/**
 * Stripe Customer Verification Service
 * 
 * Re-exports the full Stripe verifier implementation.
 * See stripe-verifier-impl.ts for the complete implementation.
 */

export { StripeVerifier, StripeVerificationError } from './stripe-verifier-impl.js';
