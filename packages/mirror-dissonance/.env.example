# ─── Cloud Provider ────────────────────────────────────────────
# Options: aws | gcp | local (default: local)
CLOUD_PROVIDER=local

# ─── AWS (only when CLOUD_PROVIDER=aws) ───────────────────────
AWS_REGION=us-east-1
DYNAMODB_FP_TABLE=phase-mirror-fp-events
DYNAMODB_CONSENT_TABLE=phase-mirror-consent
DYNAMODB_BLOCK_TABLE=phase-mirror-blocks
SSM_NONCE_PARAM=/guardian/production/redaction_nonce_v1
S3_DRIFT_BUCKET=phase-mirror-drift-baselines

# ─── GCP (only when CLOUD_PROVIDER=gcp) ───────────────────────
GCP_PROJECT_ID=my-project
GCP_REGION=us-central1

# ─── Local (only when CLOUD_PROVIDER=local) ───────────────────
LOCAL_DATA_DIR=.phase-mirror-data
