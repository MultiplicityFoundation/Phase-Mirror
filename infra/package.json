{
  "name": "@phase-mirror/infra-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Phase Mirror infrastructure — Lambda functions",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "tsc",
    "test": "jest --config jest.config.cjs",
    "test:coverage": "jest --config jest.config.cjs --coverage"
  },
  "dependencies": {
    "@phase-mirror/pro": "workspace:*",
    "@mirror-dissonance/core": "workspace:*"
  },
  "optionalDependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/util-dynamodb": "^3.500.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.10.0"
  }
}
